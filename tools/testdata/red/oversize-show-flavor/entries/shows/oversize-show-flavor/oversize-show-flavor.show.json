{
  "schemaVersion": 1,
  "name": "Oversize Show Flavor",
  "tagline": "Tagline stays comfortably within budget.",
  "flavor": "Prompt-only flavor text pushed all the way out to exactly twice its one-times budget so the catalog lint's hard tier rejects this show manifest outright, proving the two-times ceiling actually bites at the boundary itself. Prompt-only flavor text pushed all the way out to exactly twice its one-times budget so the catalog lint's hard tier rejects this show manifest outright, proving the two-times ceiling actually bites at the boundary itself. Prompt-only flavor text pushed all the way out to exactly twice its one-times budget so the catalog lint's hard tier rejects this show manifest outright, proving the two-times ceiling actually bites at the boundary itself. Prompt-only flavor text pushed all the way out to exactly twice its one-times budget so the catalog lint's hard tier rejects this s"
}
